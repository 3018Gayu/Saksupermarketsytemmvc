@model Saksupermarketsytemmvc.web.Models.Orders

<h2>Order Details</h2>

<dl class="row">
    <dt class="col-sm-3">Order ID</dt>
    <dd class="col-sm-9">@Model.OrderId</dd>

    <dt class="col-sm-3">Customer</dt>
    <dd class="col-sm-9">@Model.Customer?.CustomerName</dd>

    <dt class="col-sm-3">Order Date</dt>
    <dd class="col-sm-9">@Model.OrderDate?.ToString("yyyy-MM-dd")</dd>

    <dt class="col-sm-3">Total Amount</dt>
    <dd class="col-sm-9">@Model.TotalAmount?.ToString("C")</dd>

    <dt class="col-sm-3">Tax Amount</dt>
    <dd class="col-sm-9">@Model.TaxAmount?.ToString("C")</dd>

    <dt class="col-sm-3">Discount</dt>
    <dd class="col-sm-9">@Model.Discount?.ToString("C")</dd>

    <dt class="col-sm-3">Net Amount</dt>
    <dd class="col-sm-9">@Model.NetAmount?.ToString("C")</dd>

    <dt class="col-sm-3">Invoice No</dt>
    <dd class="col-sm-9">@Model.InvoiceNo</dd>
</dl>

<a asp-action="Index" class="btn btn-secondary">Back</a>
